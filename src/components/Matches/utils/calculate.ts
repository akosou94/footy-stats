export const avg = (count: number, length: number) => Number((count / length).toFixed(2));

// export const ratingAttackTeam = (powerAttackH, powerDefenceA, avgGoalsLeague) =>
// 	Number(powerAttackH) * Number(powerDefenceA) * Number(avgGoalsLeague);
